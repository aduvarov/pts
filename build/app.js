"use strict";
class User {
    constructor(name) {
        this.name = name;
    }
}
function createUser(user) {
    //logic
    // const defaultUser = new User('default')
    // defaultUser.role = undefined
    switch (user.role) {
        // case 'admin':
        //     const a = 7
        case 'user':
            return true;
        // const c = 1
    }
}
const c = {};
const d = c['drive'];
// c.drive
